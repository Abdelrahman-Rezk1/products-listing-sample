export enum MappingEntity {
  Product = 'Product',
}

export enum MappingDirection {
  ToEntity = 'toEntity',
  ToZoho = 'toZoho',
}

export enum TransformMethod {
  ToNumber = 'toNumber',
  ToString = 'toString',
  NullIfEmpty = 'nullIfEmpty',
  DefaultIfNull = 'defaultIfNull',
}
